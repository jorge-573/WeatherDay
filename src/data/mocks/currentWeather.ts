import type { CurrentWeatherSnapshot } from '../../types/weather'

export const currentWeatherMock: CurrentWeatherSnapshot = {
  location: 'San Francisco, CA',
  temperature: 72,
  condition: 'Mostly Sunny',
  high: 76,
  low: 58,
  feelsLike: 74,
}
